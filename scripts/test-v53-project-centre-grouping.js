'use strict';

const assert=require('assert/strict');
const crypto=require('crypto');
const fsp=require('fs/promises');
const os=require('os');
const path=require('path');
const {spawn}=require('child_process');
const XLSX=require('xlsx');

const root=path.resolve(__dirname,'..'),port=22100+Math.floor(Math.random()*400),base=`http://127.0.0.1:${port}`;
const username='group-reviewer@example.edu',password='Strong-Group-Test-Password!';
const headers={authorization:`Basic ${Buffer.from(`${username}:${password}`).toString('base64')}`};
const file=(name)=>({storedName:name,originalName:name,mimeType:'application/octet-stream',size:1});
const row=(n,centre,group='1')=>({originalSn:String(n),name:`Student ${n}`,registrationNo:`BEP/${centre}/${String(n).padStart(3,'0')}`,groupNo:group,totalScore:String(70+n)});
function record(id,status,groupCount,rows,workCount){return {id,portalType:'project-work',department:'education',departmentName:'Department of Education Programmes',reference:`PWORK-${id}`,submittedAt:'2026-09-20T09:00:00.000Z',fullName:'Dr Group Supervisor',email:'group.supervisor@example.edu',phone:'0240000000',groupCount:String(groupCount),claimedGroupCount:groupCount,studyCentres:['WESLEY COLLEGE OF EDUCATION, KUMASI','KUMASI ANGLICAN SECONDARY SCHOOL'],studentStream:'distance',projectStream:'distance',classificationVersion:0,projectCentreDecisions:[],projectCentreDecisionHistory:[],reviewStatus:status,reviewedAt:status==='approved'?'2026-09-20T10:00:00.000Z':null,reviewedBy:status==='approved'?'Reviewer':'',reviewHistory:[],scoreSheet:{rows},scoreReviewExcludedRows:[],files:{claimForm:file(`${id}-claim.pdf`),reportFile:file(`${id}-report.docx`),scoresFile:file(`${id}-scores.xlsx`),completedWork:Array.from({length:workCount},(_,i)=>file(`${id}-work-${i+1}.docx`))}};}
async function request(url,options={}){return fetch(`${base}${url}`,options)}
async function json(url,options={}){const response=await request(url,options),data=await response.json().catch(()=>({}));assert.equal(response.status,options.expectedStatus||200,`${url}: ${data.error||response.status}`);return data;}

async function main(){
  const storage=await fsp.mkdtemp(path.join(os.tmpdir(),'ucc-v53-groups-')),dataDir=path.join(storage,'data');await fsp.mkdir(dataDir,{recursive:true});
  const autoRows=[row(1,'AS/01'),row(2,'AS/01'),row(3,'AS/01'),row(4,'AS/01'),row(5,'AS/02'),row(6,'AS/02')];
  const distinctRows=[row(11,'AS/01'),row(12,'AS/01'),row(13,'AS/01'),row(14,'AS/02'),row(15,'AS/02'),row(16,'AS/02')];
  const reviewRows=[row(21,'AS/01'),row(22,'AS/01'),row(23,'AS/02'),row(24,'AS/02')];
  const records=[record('auto','approved',1,autoRows,1),record('distinct','approved',2,distinctRows,2),record('review','pending',2,reviewRows,2),record('review-merge','pending',1,reviewRows,1)];
  const salt=crypto.randomBytes(16).toString('hex'),passwordHash=crypto.scryptSync(password,salt,64).toString('hex'),account={id:'group-admin',name:'Group Reviewer',email:username,username,role:'administrator',departments:['education'],sections:['project-work'],units:[],active:true,passwordSalt:salt,passwordHash};
  await Promise.all([fsp.writeFile(path.join(dataDir,'submissions.json'),JSON.stringify(records,null,2)),fsp.writeFile(path.join(dataDir,'admin-users.json'),JSON.stringify([account],null,2)),fsp.writeFile(path.join(dataDir,'support-tickets.json'),'[]')]);
  const child=spawn(process.execPath,['server.js'],{cwd:root,env:{...process.env,PORT:String(port),STORAGE_DIR:storage,GMAIL_CLIENT_ID:'',GMAIL_CLIENT_SECRET:'',GMAIL_REFRESH_TOKEN:'',GMAIL_SENDER_EMAIL:'',DEVELOPER_ADMIN_PASSWORD:'test-developer-secret',SUPPORT_STATUS_TOKEN_SECRET:'independent-v53-test-secret'},stdio:['ignore','pipe','pipe']});let output='';child.stdout.on('data',chunk=>{output+=chunk});child.stderr.on('data',chunk=>{output+=chunk});
  try{
    const deadline=Date.now()+15000;while(!output.includes('listening on')&&Date.now()<deadline)await new Promise(resolve=>setTimeout(resolve,50));assert.match(output,/listening on/,output);
    const uploadWb=XLSX.utils.book_new();XLSX.utils.book_append_sheet(uploadWb,XLSX.utils.aoa_to_sheet([['S/N','NAME','REGISTRATION NO.','GROUP NO.','TOTAL SCORE'],...reviewRows.map((item,index)=>[index+1,item.name,item.registrationNo,item.groupNo,item.totalScore])]),'Scores');const uploadForm=new FormData();for(const [key,value] of Object.entries({department:'education',title:'Dr',firstName:'Upload',lastName:'Tester',phone:'0240000000',email:'upload.tester@example.edu',groupCount:'1',studentStream:'distance',claimantDeclaration:'yes'}))uploadForm.set(key,value);uploadForm.append('studyCentre','WESLEY COLLEGE OF EDUCATION, KUMASI');uploadForm.append('studyCentre','KUMASI ANGLICAN SECONDARY SCHOOL');uploadForm.set('scoresFile',new Blob([XLSX.write(uploadWb,{type:'buffer',bookType:'xlsx'})]),'scores.xlsx');uploadForm.set('claimForm',new Blob(['claim']),'claim.pdf');uploadForm.set('reportFile',new Blob(['report']),'report.docx');uploadForm.append('completedWork',new Blob(['work']),'work.docx');let response=await request('/api/project-work',{method:'POST',body:uploadForm});assert.equal(response.status,201,`ambiguous but administratively mergeable submission should be accepted: ${await response.text()}`);
    let detail=await json('/api/admin/education/submissions/auto',{headers});assert.equal(detail.groupValidation.scoreSheetGroupCount,1);assert.equal(detail.groupValidation.transferExceptions.length,1);assert.equal(detail.groupValidation.transferExceptions[0].minorityStudentCount,2);assert.equal(detail.groupValidation.transferExceptions[0].reportingCentreCode,'AS/01');assert.equal(detail.paymentUnits.length,1);
    detail=await json('/api/admin/education/submissions/distinct',{headers});assert.equal(detail.groupValidation.scoreSheetGroupCount,2,'two normal centre groups with the same group number must remain distinct');assert.equal(detail.groupValidation.transferExceptions.length,0);
    detail=await json('/api/admin/education/submissions/review',{headers});assert.equal(detail.groupValidation.requiresCentreReview,true);assert.equal(detail.groupValidation.centreReviewCases.length,1);
    response=await request('/api/admin/education/project-work/review/review',{method:'POST',headers:{...headers,'content-type':'application/json'},body:JSON.stringify({status:'approved'})});assert.equal(response.status,409,'ambiguous centre patterns must not be approved before an admin decision');
    const centreCase=detail.groupValidation.centreReviewCases[0];await json('/api/admin/education/project-work/review/centre-decision',{method:'POST',headers:{...headers,'content-type':'application/json'},body:JSON.stringify({caseKey:centreCase.caseKey,action:'keep-distinct',reason:'Supporting projects confirm two separate centre groups.'})});
    await json('/api/admin/education/project-work/review/review',{method:'POST',headers:{...headers,'content-type':'application/json'},body:JSON.stringify({status:'approved'})});
    detail=await json('/api/admin/education/submissions/review-merge',{headers});assert.deepEqual(detail.groupValidation.possibleScoreSheetGroupCounts,[1,2]);const mergeCase=detail.groupValidation.centreReviewCases[0];await json('/api/admin/education/project-work/review-merge/centre-decision',{method:'POST',headers:{...headers,'content-type':'application/json'},body:JSON.stringify({caseKey:mergeCase.caseKey,action:'merge-predominant',reportingCentreCode:'AS/01',reason:'The attached project confirms one four-student group working at AS/01.'})});detail=await json('/api/admin/education/submissions/review-merge',{headers});assert.equal(detail.groupValidation.scoreSheetGroupCount,1);assert.equal(detail.groupValidation.transferExceptions[0].resolution,'admin-confirmed-predominant');await json('/api/admin/education/project-work/review-merge/review',{method:'POST',headers:{...headers,'content-type':'application/json'},body:JSON.stringify({status:'approved'})});
    response=await request('/api/admin/education/export/project-scores.xlsx',{headers});assert.equal(response.status,200);const wb=XLSX.read(await response.arrayBuffer(),{type:'array'}),matrix=XLSX.utils.sheet_to_json(wb.Sheets[wb.SheetNames[0]],{header:1});const header=matrix[0],centreIndex=header.indexOf('STUDY CENTRE'),registrationIndex=header.indexOf('REGISTRATION NO.');const autoExport=matrix.slice(1).filter(values=>String(values[registrationIndex]||'').startsWith('BEP/AS/'));
    const transferredRows=autoExport.filter(values=>['BEP/AS/02/005','BEP/AS/02/006'].includes(values[registrationIndex]));assert.equal(transferredRows.length,2);assert.ok(transferredRows.every(values=>values[centreIndex]==='WESLEY COLLEGE OF EDUCATION, KUMASI'),'minority-code students must export under the predominant reporting centre');
    console.log('v53 Project Work predominant-centre grouping checks passed.');
  }finally{child.kill('SIGTERM');await fsp.rm(storage,{recursive:true,force:true});}
}
main().catch(error=>{console.error(error);process.exitCode=1});
