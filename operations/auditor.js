const department=location.pathname.split('/').filter(Boolean)[1]||'';let rows=[];const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));const fmt=v=>{if(!v)return '';try{return new Date(v).toLocaleString()}catch{return v}};
async function get(url){const r=await fetch(url),d=await r.json().catch(()=>({}));if(r.status===401){location.href=`/admin-login.html?department=${encodeURIComponent(department)}&next=${encodeURIComponent(location.pathname)}`;throw new Error('Authentication required.')}if(!r.ok)throw new Error(d.error||'Request failed.');return d;}
function matches(x){const q=document.getElementById('search').value.trim().toLowerCase();if(!q)return true;return [x.reference,x.supervisorName,x.email,x.studyCentres,x.payrollStatusLabel,x.payrollNote].join(' ').toLowerCase().includes(q)}
function render(){const list=rows.filter(matches);document.getElementById('count').textContent=list.length;document.getElementById('empty').hidden=!!list.length;document.getElementById('rows').innerHTML=list.map((x,i)=>`<tr><td>${i+1}</td><td><strong>${esc(x.reference)}</strong><br><small>${esc(x.studentStream)}</small></td><td>${esc(x.supervisorName)}<br><small>${esc(x.email)}</small></td><td>${esc(x.studyCentres)}</td><td>${esc(fmt(x.approvedAt))}<br><small>${esc(x.approvedBy)}</small></td><td>${esc(x.claimedGroupCount??x.claimedGroupsRaw)}</td><td>${esc(x.scoreSheetGroupCount)}</td><td>${esc(x.completedProjectWorkCount)}</td><td class="${x.groupValidation?.valid?'check-ok':'check-bad'}">${x.groupValidation?.valid?'MATCH':'MISMATCH'}</td><td>${x.claimFormPresent?`<button class="btn" onclick="openClaim('${esc(x.id)}','${esc(x.reference)}')">Preview</button>`:'<span class="check-bad">Missing</span>'}</td><td><span class="status ${esc(x.payrollStatus)}">${esc(x.payrollStatusLabel)}</span></td><td class="note">${esc(x.payrollNote||'')}</td><td>${esc(fmt(x.payrollUpdatedAt))}<br><small>${esc(x.payrollUpdatedBy||'')}</small></td></tr>`).join('')}
window.openClaim=(id,ref)=>{document.getElementById('claimTitle').textContent=`Claim Form · ${ref}`;document.getElementById('claimFrame').src=`/api/admin/${encodeURIComponent(department)}/submissions/${encodeURIComponent(id)}/claim-preview`;document.getElementById('claimDialog').showModal()};
async function load(){const [info,data]=await Promise.all([get(`/api/admin/${encodeURIComponent(department)}/info`),get(`/api/auditor/${encodeURIComponent(department)}/claims`)]);rows=data;document.getElementById('departmentName').textContent=info.departmentName;document.getElementById('adminLink').href=`/admin/${encodeURIComponent(department)}`;document.getElementById('payrollLink').href=`/payroll/${encodeURIComponent(department)}`;document.getElementById('approvedRegister').href=`/api/auditor/${encodeURIComponent(department)}/approved-register.xlsx`;document.getElementById('auditRegister').href=`/api/auditor/${encodeURIComponent(department)}/register.xlsx`;render()}
document.getElementById('search').oninput=render;document.getElementById('closeClaim').onclick=()=>{document.getElementById('claimFrame').src='about:blank';document.getElementById('claimDialog').close()};document.getElementById('logoutBtn').onclick=async()=>{await fetch('/api/admin-logout',{method:'POST'}).catch(()=>{});location.href='/'};load().catch(e=>{console.error(e);alert(e.message)});
